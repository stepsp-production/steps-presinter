// /vendor/livekit-client.umd.min.js
(function () {
  if (window.LivekitClient || window.livekitClient || window.LiveKitClient) return;
  var s = document.createElement('script');
  s.src = 'https://cdn.jsdelivr.net/npm/livekit-client@latest/dist/livekit-client.umd.min.js';
  s.async = true;
  s.onload = function(){
    var lk = window.LivekitClient || window.livekitClient || window.LiveKitClient;
    if (lk) window.LivekitClient = lk;
    console.log('[vendor] livekit-client UMD ready');
  };
  s.onerror = function(){ console.error('Failed to load livekit-client UMD'); };
  document.head.appendChild(s);
})();
